function countCoffeeList0(type)  {
	  // 결과를 표시할 element
	  const resultCount = document.getElementById('coffeeCount0');
	  const resultAmount = document.getElementById('coffeeAmount0');
	  
	  // 현재 화면에 표시된 값
	  let number = resultCount.innerText;
	  
	  // 더하기/빼기
	  if(type === 'plus') {
	    number = parseInt(number) + 1;
	  }else if(type === 'minus' && number > 0)  {
	    number = parseInt(number) - 1;
	  }
	  
	  // 결과 출력
	  resultCount.innerText = number;
	  resultAmount.value = resultCount.innerText
	}
	
function countCoffeeList1(type)  {
	  // 결과를 표시할 element
	  const resultCount = document.getElementById('coffeeCount1');
	  const resultAmount = document.getElementById('coffeeAmount1');
	  
	  // 현재 화면에 표시된 값
	  let number = resultCount.innerText;
	  
	  // 더하기/빼기
	  if(type === 'plus') {
	    number = parseInt(number) + 1;
	  }else if(type === 'minus' && number > 0)  {
	    number = parseInt(number) - 1;
	  }
	  
	  // 결과 출력
	  resultCount.innerText = number;
	  resultAmount.value = resultCount.innerText
	}

function countCoffeeList2(type)  {
	  // 결과를 표시할 element
	  const resultCount = document.getElementById('coffeeCount2');
	  const resultAmount = document.getElementById('coffeeAmount2');
	  
	  // 현재 화면에 표시된 값
	  let number = resultCount.innerText;
	  
	  // 더하기/빼기
	  if(type === 'plus') {
	    number = parseInt(number) + 1;
	  }else if(type === 'minus' && number > 0)  {
	    number = parseInt(number) - 1;
	  }
	  
	  // 결과 출력
	  resultCount.innerText = number;
	  resultAmount.value = resultCount.innerText
	}
	
function countCoffeeList3(type)  {
	  // 결과를 표시할 element
	  const resultCount = document.getElementById('coffeeCount3');
	  const resultAmount = document.getElementById('coffeeAmount3');
	  
	  // 현재 화면에 표시된 값
	  let number = resultCount.innerText;
	  
	  // 더하기/빼기
	  if(type === 'plus') {
	    number = parseInt(number) + 1;
	  }else if(type === 'minus' && number > 0)  {
	    number = parseInt(number ) - 1;
	  }
	  
	  // 결과 출력
	  resultCount.innerText = number;
	  resultAmount.value = resultCount.innerText
	}
	
function countCoffeeList4(type)  {
	  // 결과를 표시할 element
	  const resultCount = document.getElementById('coffeeCount4');
	  const resultAmount = document.getElementById('coffeeAmount4');
	  
	  // 현재 화면에 표시된 값
	  let number = resultCount.innerText;
	  
	  // 더하기/빼기
	  if(type === 'plus') {
	    number = parseInt(number) + 1;
	  }else if(type === 'minus' && number > 0)  {
	    number = parseInt(number) - 1;
	  }
	  
	  // 결과 출력
	  resultCount.innerText = number;
	  resultAmount.value = resultCount.innerText
	}
	